{% comment %}
  modules/accela_construct/queries/inspections/related/build

  Builds the request path for GET /v4/inspections/{id}/related.

  Params:
    object - Hash with `id` (see inspections/related.liquid)

  Returns object with `path` added.
{% endcomment %}

{% liquid
  assign object.path = "/inspections/{{ object.id | url_encode }}/related"
  return object
%}
