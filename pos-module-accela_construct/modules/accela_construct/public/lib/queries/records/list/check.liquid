{% comment %}
  modules/accela_construct/queries/records/list/check
  No required params for this endpoint - every filter Accela documents
  for "Get All Records" is individually optional. Kept for structural
  consistency with the build/check split used across all queries.
{% endcomment %}

{% liquid
  assign object.valid = true
  assign object.errors = {}
  return object
%}
