{% comment %}
  modules/accela_construct/queries/records/addresses/list/build

  Builds the request path for GET /v4/records/{recordId}/addresses.

  Params:
    object - Hash with `record_id` (see records/addresses/list.liquid)

  Returns object with `path` added.
{% endcomment %}

{% liquid
  assign object.path = "/records/{{ object.record_id | url_encode }}/addresses"
  return object
%}
