{% comment %}
  modules/accela_construct/queries/records/votes/build

  Builds the request path for GET /v4/records/{recordId}/votes.

  Params:
    object - Hash with `record_id` (see records/votes.liquid)

  Returns object with `path` added.
{% endcomment %}

{% liquid
  assign object.path = "/records/{{ object.record_id | url_encode }}/votes"
  return object
%}
