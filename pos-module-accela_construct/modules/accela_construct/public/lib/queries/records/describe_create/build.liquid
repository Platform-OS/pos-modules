{% comment %}
  modules/accela_construct/queries/records/describe_create/build

  Builds the request path for GET /v4/records/describe/create?type={type}.

  Params:
    object - Hash with `type` (see records/describe_create.liquid)

  Returns object with `path` added.
{% endcomment %}

{% liquid
  assign object.path = "/records/describe/create?type={{ object.type | url_encode }}"
  return object
%}
