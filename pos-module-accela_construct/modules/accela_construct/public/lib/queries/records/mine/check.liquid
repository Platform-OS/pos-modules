{% doc %}
  No required params for this endpoint - offset/limit/fields are all
  optional. Kept for structural consistency with the build/check split
  used across all queries.
{% enddoc %}

{% liquid
  assign object.valid = true
  assign object.errors = {}
  return object
%}
