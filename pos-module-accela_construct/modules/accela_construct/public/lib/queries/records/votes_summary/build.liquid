{% comment %}
  modules/accela_construct/queries/records/votes_summary/build

  Builds the request path for GET /v4/records/{recordId}/votes/summary.

  Params:
    object - Hash with `record_id` (see records/votes_summary.liquid)

  Returns object with `path` added.
{% endcomment %}

{% liquid
  assign object.path = "/records/{{ object.record_id | url_encode }}/votes/summary"
  return object
%}
