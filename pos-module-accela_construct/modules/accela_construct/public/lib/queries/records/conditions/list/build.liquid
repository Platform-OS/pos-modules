{% comment %}
  modules/accela_construct/queries/records/conditions/list/build

  Builds the request path for GET /v4/records/{recordId}/conditions.

  Params:
    object - Hash with `record_id` (see records/conditions/list.liquid)

  Returns object with `path` added.
{% endcomment %}

{% liquid
  assign object.path = "/records/{{ object.record_id | url_encode }}/conditions"
  return object
%}
