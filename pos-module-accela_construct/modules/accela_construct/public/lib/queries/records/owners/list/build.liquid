{% comment %}
  modules/accela_construct/queries/records/owners/list/build

  Builds the request path for GET /v4/records/{recordId}/owners.

  Params:
    object - Hash with `record_id` (see records/owners/list.liquid)

  Returns object with `path` added.
{% endcomment %}

{% liquid
  assign object.path = "/records/{{ object.record_id | url_encode }}/owners"
  return object
%}
