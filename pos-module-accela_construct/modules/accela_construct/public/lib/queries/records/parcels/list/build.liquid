{% comment %}
  modules/accela_construct/queries/records/parcels/list/build

  Builds the request path for GET /v4/records/{recordId}/parcels.

  Params:
    object - Hash with `record_id` (see records/parcels/list.liquid)

  Returns object with `path` added.
{% endcomment %}

{% liquid
  assign object.path = "/records/{{ object.record_id | url_encode }}/parcels"
  return object
%}
