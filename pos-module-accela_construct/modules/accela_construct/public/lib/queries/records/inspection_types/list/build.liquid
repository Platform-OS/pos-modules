{% comment %}
  modules/accela_construct/queries/records/inspection_types/list/build

  Builds the request path for GET /v4/records/{recordIds}/inspectionTypes.

  Params:
    object - Hash with `record_ids` (see inspection_types/list.liquid)

  Returns object with `path` added.
{% endcomment %}

{% liquid
  assign object.path = "/records/{{ object.record_ids | url_encode }}/inspectionTypes"
  return object
%}
