{% comment %}
  modules/accela_construct/queries/records/workflow_tasks/comments_histories/build

  Builds the request path for GET /v4/records/{recordId}/workflowTasks/comments/histories.

  Params:
    object - Hash with `record_id` (see records/workflow_tasks/comments_histories.liquid)

  Returns object with `path` added.
{% endcomment %}

{% liquid
  assign object.path = "/records/{{ object.record_id | url_encode }}/workflowTasks/comments/histories"
  return object
%}
