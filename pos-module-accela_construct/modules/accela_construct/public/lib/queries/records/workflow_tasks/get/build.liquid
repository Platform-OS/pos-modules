{% comment %}
  modules/accela_construct/queries/records/workflow_tasks/get/build

  Builds the request path for GET /v4/records/{recordId}/workflowTasks/{id}.

  Params:
    object - Hash with `record_id`, `id` (see records/workflow_tasks/get.liquid)

  Returns object with `path` added.
{% endcomment %}

{% liquid
  assign object.path = "/records/{{ object.record_id | url_encode }}/workflowTasks/{{ object.id | url_encode }}"
  return object
%}
