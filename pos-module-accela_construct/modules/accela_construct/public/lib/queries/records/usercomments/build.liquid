{% comment %}
  modules/accela_construct/queries/records/usercomments/build

  Builds the request path for GET /v4/records/{recordId}/usercomments.

  Params:
    object - Hash with `record_id` (see records/usercomments.liquid)

  Returns object with `path` added.
{% endcomment %}

{% liquid
  assign object.path = "/records/{{ object.record_id | url_encode }}/usercomments"
  return object
%}
