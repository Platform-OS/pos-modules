{% doc %}
  @description Renders the Cloudflare Turnstile widget (loader script + widget div) into a form.
  @param {string} site_key - Turnstile public site key.
  @param {object} [options] - Widget options: theme (light|dark|auto), size (normal|flexible|compact),
    action, cdata, appearance (always|execute|interaction-only), language, tabindex, callback (JS fn name),
    response_field_name (default cf-turnstile-response), class_name (extra CSS classes), html_id (id attribute).
{% enddoc %}
{%- liquid
  assign response_field = options['response_field_name'] | default: 'cf-turnstile-response'
-%}
{%- unless context.exports.captcha.turnstile_script_loaded -%}
<script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer></script>
{%- assign turnstile_script_loaded = true -%}
{%- export turnstile_script_loaded, namespace: 'captcha' -%}
{%- endunless -%}
<div class="cf-turnstile{% if options['class_name'] != blank %} {{ options['class_name'] }}{% endif %}"{% if options['html_id'] != blank %} id="{{ options['html_id'] }}"{% endif %} data-sitekey="{{ site_key }}"{% if options['theme'] != blank %} data-theme="{{ options['theme'] }}"{% endif %}{% if options['size'] != blank %} data-size="{{ options['size'] }}"{% endif %}{% if options['action'] != blank %} data-action="{{ options['action'] }}"{% endif %}{% if options['cdata'] != blank %} data-cdata="{{ options['cdata'] }}"{% endif %}{% if options['appearance'] != blank %} data-appearance="{{ options['appearance'] }}"{% endif %}{% if options['language'] != blank %} data-language="{{ options['language'] }}"{% endif %}{% if options['tabindex'] != blank %} data-tabindex="{{ options['tabindex'] }}"{% endif %}{% if options['callback'] != blank %} data-callback="{{ options['callback'] }}"{% endif %} data-response-field-name="{{ response_field }}"></div>
