{% liquid
  assign state = object.state | default: "pending"
  assign object = {"account_id": object.account_id, "reference_id": object.reference_id, "state": state, "last_errors": object.last_errors, "stripe_account_name": object.stripe_account_name, "data": object.data}

  return object
%}
