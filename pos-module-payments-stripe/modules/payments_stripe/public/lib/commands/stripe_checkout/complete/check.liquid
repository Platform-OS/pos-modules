{% liquid
  # platformos-check-disable MetadataParamsCheck
  assign c = { "errors": {}, "valid": true }

  function c = 'modules/core/validations/presence', c: c, object: object.data.object, field_name: 'client_reference_id'

  assign ids = object.data.object['client_reference_id'] | split: ','
  function c = 'modules/core/validations/exist_in_db', c: c, field_name: 'transaction_id', table: 'modules/payments/transaction', ids: ids, exclude_name: nil, exclude_value: nil, key: nil, not_ids: nil, property_name: nil, property_value: nil, scope_name: nil, scope_value: nil

  assign object.valid = c.valid
  assign object.errors = c.errors

  return object
  # platformos-check-enable MetadataParamsCheck
 %}
