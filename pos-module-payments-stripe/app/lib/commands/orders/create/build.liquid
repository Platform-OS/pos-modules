{% liquid
  assign idempotency_key = '' | uuid
  assign object = {
    "profile_id": object.profile_id,
    "amount": object.amount,
    "currency": object.currency,
    "idempotency_key": idempotency_key
  }

  return object
%}
