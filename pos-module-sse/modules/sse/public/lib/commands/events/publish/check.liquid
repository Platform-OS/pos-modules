{% doc %}
  @param {object} object - Built event object
{% enddoc %}
{% liquid
  assign c = { "errors": {}, "valid": true }

  function c = 'modules/core/validations/presence', c: c, object: object, field_name: 'channel'
  function c = 'modules/core/validations/presence', c: c, object: object, field_name: 'data'

  assign object.valid = c.valid
  assign object.errors = c.errors

  return object
%}
