.pos
tmp/
.pos-supervisor/
pos-supervisor.jsonl

# Installed module dependencies (downloaded via `pos-cli modules install`).
# The distributed module lives in modules/captchas_recaptcha3 and stays tracked; deps do not.
modules/captchas/
modules/tests/
