{% liquid
  function object = 'modules/payments_example_gateway/helpers/pay_object', gateway_params: gateway_params, transaction: transaction
  if object.valid
    return object.url
  else
    log object, type: 'ERROR: payments_example pay_url'
    return null
  endif
%}
