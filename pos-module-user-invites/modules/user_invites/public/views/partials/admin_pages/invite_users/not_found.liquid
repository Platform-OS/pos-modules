{% comment %}
  @prompt: Renders the "CSV not found" admin message with a back link.
{% endcomment %}
<div class="container">
  <h2 class="pb-4 font-extrabold text-3xl">{{ 'modules/user_invites/admin.invite_users.not_found_heading' | t }}</h2>
  <a href="/admin/community/invite_users" class="underline">{{ 'modules/user_invites/admin.invite_users.back' | t }}</a>
</div>
