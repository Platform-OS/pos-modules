query find($id: ID!) {
  records(
    per_page: 1
    page: 1
    filter: {
      id: { value: $id }
      table: {
        value: "modules/user_invites/user_invite"
      }
    }
  ) {
    results {
      id
      properties
    }
  }
}
